name = "bob"
nickname = "vagabond"
# .reverse reverses the characters of a string

puts ".reverse reverses the order of a string, for example #{name} becomes #{name.reverse}.  Granted, #{name} is a bad example of this..."

# .length method does counts the number of characters in string

puts "#{name} has #{name.length} letters in it."

# .prepend with reverse the order of 2 strings

#puts ".prepend will reverse the order of 2 strings, I can take #{name} and #{nickname} and make them into #{name.prepend("Vagabond")}"
puts "#{name.chars}"
